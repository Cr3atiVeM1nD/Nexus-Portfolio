Ich kann den TypeScript-Compiler nicht ausführen, da ich keinen Shell‑Zugriff habe.
Bitte führen Sie in Ihrem Terminal (cmd.exe) folgenden Befehl aus:

cd C:\Dev\Nexus Portfolio && npx tsc --noEmit 2>&1

Zeigen Sie mir die Ausgabe, damit ich Ihnen bei eventuellen Fehlern helfen kann.
